#include <bits/stdc++.h>
using namespace std;

int N;

void solve() {
	for (int i = 0; i < N; i++) {
		cout << i + 1 << "\n";
	}
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cin >> N;
	solve();
	return EXIT_SUCCESS;
}
