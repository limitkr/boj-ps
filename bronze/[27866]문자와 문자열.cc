#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  string str;
  int pos;
  cin >> str >> pos;
  cout << str[pos - 1] << "\n";

  return EXIT_SUCCESS;
}